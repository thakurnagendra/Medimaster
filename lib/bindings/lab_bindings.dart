import 'package:get/get.dart';

class LabBinding extends Bindings {
  @override
  void dependencies() {
    // Initialize lab-related controllers and services here
    // For example:
    // Get.lazyPut(() => LabController());
    // Get.lazyPut(() => LabService());
  }
}
