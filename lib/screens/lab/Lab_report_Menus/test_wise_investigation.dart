import 'package:flutter/material.dart';

class LabReportMenuTestWiseInvestigationScreen extends StatelessWidget {
  const LabReportMenuTestWiseInvestigationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Test Wise Investigation'),
        backgroundColor: const Color(0xFF4CAF50),
      ),
      body: const Center(
        child: Text('Test Wise Investigation Screen - Coming Soon'),
      ),
    );
  }
}
