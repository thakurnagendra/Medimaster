import 'package:flutter/material.dart';

class LabReportMenuAgentWiseBillingDetailScreen extends StatelessWidget {
  const LabReportMenuAgentWiseBillingDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Agent Wise Billing Detail'),
        backgroundColor: const Color(0xFF4CAF50),
      ),
      body: const Center(
        child: Text('Agent Wise Billing Detail Screen - Coming Soon'),
      ),
    );
  }
}
